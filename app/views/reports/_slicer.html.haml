.status.clearfix
  - @slicer.cuts.each do |cut|
    .fl.cut
      %em= cut[0]
      = cut[1].join(" ")
      %a{:href => create_report_path(params.merge(:cut => @slicer.without(cut[0]).to_param))}
        = image_tag "status_remove.png"