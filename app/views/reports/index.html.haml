%h1 Verejné obstarávania

== Hodnota zmlúv: #{@hodnota_zmluv}
%br
== Počet zmlúv: #{@pocet_zmluv}
%br
%br

.clearfix
  .left_column
    %h2 Top dodávatelia
    = DataView::Table.new(@top_10_dodavatelia).as_html
  .right_column
    %h2 Top obstáravatelia
    = DataView::Table.new(@top_10_obstaravatelia).as_html

.clearfix
  .left_column
    = DataView::PieChart.new(@top_10_dodavatelia, {:legend => 0, :value => 1}).as_html
  .right_column
    = DataView::PieChart.new(@top_10_obstaravatelia, {:legend => 0, :value => 1}).as_html

%br
%br
    
.clearfix
  .left_column
    %h2 Typy tovarov
    = DataView::Table.new(@typy_tovarov).as_html
  .right_column
    %h2 Typy tovarov (graf)
    = DataView::PieChart.new(@typy_tovarov, {:legend => 0, :value => 1}).as_html

%br
%br

.clearfix
  .left_column
    %h2 Druhy postupov
    = DataView::Table.new(@druh_postupu).as_html
  .right_column
    %h2 Druhy postupov (graf)
    = DataView::PieChart.new(@druh_postupu, {:legend => 0, :value => 1}).as_html
    
%br
%br

%h2 Vývoj v priebehu roka 

= DataView::BarChart.new(@posledny_rok, {:legend => 0, :value => 1}).as_html